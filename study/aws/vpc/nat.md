# NAT(Network Address Translation)

## 1. NAT란?
- NAT(Network Address Translation)은 사설 IP 주소를 공인 IP 주소로 변환하는 기술이다.
- 사설 IP 주소는 인터넷에 연결되지 않기 때문에, 사설 IP 주소를 공인 IP 주소로 변환하여 인터넷에 연결할 수 있도록 한다.
- NAT는 라우터에 의해 수행되며, 라우터는 패킷의 출발지 주소를 변경하여 목적지로 전달한다.
- NAT는 사설 IP 주소를 공인 IP 주소로 변환하는 기술이다.
- 

